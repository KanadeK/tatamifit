[build-system]
requires = ["uv_build>=0.11.20,<0.13"]
build-backend = "uv_build"

[project]
name = "tatamifit"
version = "0.1.0"
description = "Plan and repair no-four-corners tatami-style mat layouts"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [
  { name = "KanadeK", email = "121669563+KanadeK@users.noreply.github.com" },
]
keywords = ["tatami", "tiling", "layout", "domino", "offline"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: End Users/Desktop",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Scientific/Engineering :: Mathematics",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/KanadeK/tatamifit"
Documentation = "https://github.com/KanadeK/tatamifit#readme"
Repository = "https://github.com/KanadeK/tatamifit.git"
Issues = "https://github.com/KanadeK/tatamifit/issues"
Changelog = "https://github.com/KanadeK/tatamifit/blob/main/CHANGELOG.md"

[project.scripts]
tatamifit = "tatamifit.cli:main"

[dependency-groups]
dev = [
  "mypy>=1.17,<2",
  "pytest>=8.4,<9",
  "pytest-cov>=6.2,<7",
  "ruff>=0.12,<0.13",
]

[tool.uv]
cache-dir = ".uv-cache"

[tool.pytest.ini_options]
addopts = ["--strict-config", "--strict-markers", "-p", "no:cacheprovider"]
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["tatamifit"]

[tool.coverage.report]
fail_under = 90
show_missing = true
skip_covered = true

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.11"
strict = true
files = ["src", "tests", "scripts"]
